# 📋 代码审查输出模板

## 完整审查报告模板

```markdown
## Code Review: [文件/PR 名称]

### 📊 摘要
> [1-2 句话总结] 本次审查共发现 X 个问题，其中 🔴 Critical X 个，🟡 Warning X 个，🔵 Info X 个。整体质量 [优秀/良好/需改进]。

---

### 🔴 必须修复 (Critical)

| # | 位置 | 问题 | 风险等级 | 修复建议 |
|---|------|------|----------|----------|
| 1 | `src/auth/login.ts:L42` | SQL 注入风险 | 数据泄露 | 使用参数化查询 |

**详细分析：**

```typescript
// ❌ 问题代码
const user = await db.query(
  `SELECT * FROM users WHERE email = '${email}'`
)

// ✅ 修复方案
const user = await db.query(
  'SELECT * FROM users WHERE email = ?',
  [email]
)
```

**验证方法：**
```bash
npm run test:security -- --grep "SQL injection"
```

---

### 🟡 建议优化 (Warning)

| # | 位置 | 问题 | 影响 | 优先级 |
|---|------|------|------|--------|
| 1 | `src/api/users.ts:L28` | N+1 查询问题 | 响应时间增加 300ms | 中 |

**详细分析：**

当前实现在循环中执行数据库查询，建议改为批量查询。

```typescript
// ❌ 当前实现
for (const userId of userIds) {
  const user = await db.query('SELECT * FROM users WHERE id = ?', [userId])
}

// ✅ 优化方案
const users = await db.query(
  'SELECT * FROM users WHERE id = ANY(?)',
  [userIds]
)
```

---

### 🔵 信息提示 (Info)

| # | 位置 | 建议 | 理由 |
|---|------|------|------|
| 1 | `src/utils/logger.ts` | 添加日志级别过滤 | 生产环境可减少 80% 日志量 |
| 2 | `src/config/index.ts` | 使用 dotenv 加载环境变量 | 更符合 12-Factor 原则 |

---

### ✅ 做得好的地方

- ✓ 完善的错误处理：所有异步操作都有 try/catch
- ✓ 清晰的命名：变量/函数命名语义明确
- ✓ 适当的注释：复杂逻辑有清晰的解释
- ✓ 测试覆盖：核心功能有单元测试

---

### 📈 代码质量指标

| 指标 | 当前值 | 目标值 | 状态 |
|------|--------|--------|------|
| 圈复杂度 | 8 | ≤10 | ✅ |
| 重复代码率 | 3% | ≤5% | ✅ |
| 测试覆盖率 | 78% | ≥80% | ⚠️ |
| 技术债务 | 2h | ≤4h | ✅ |

---

### 🎯 结论

**审查结果：** 🟡 请求修改

**合并条件：**
- [ ] 修复所有 🔴 Critical 问题
- [ ] 考虑优化 🟡 Warning 问题（至少 1 个）

**预计修复时间：** 30 分钟

---

### 📋 下一步行动

1. **立即修复** - 应用上述 🔴 #1 修复
2. **本次迭代** - 考虑优化 🟡 #1（性能提升明显）
3. **后续优化** - 规划 🔵 提示项（可加入技术债务 backlog）
```

## 快速审查模板（简短反馈）

```markdown
### 📋 快速审查：[文件名]

🔴 **必须修复**
- L42: SQL 注入风险 → 改用参数化查询

🟡 **建议优化**  
- L18: 日志记录敏感数据 → 脱敏处理
- L55: 缺少输入长度限制 → 添加 max 验证

✅ **优点**
- 错误处理完善
- 命名清晰

**结论：** 修复🔴问题后可合并
```

## 安全专项审查模板

```markdown
## 🔐 安全专项审查

### 认证与授权
- [ ] 身份验证逻辑正确
- [ ] 权限校验完整
- [ ] 会话管理安全

### 数据安全
- [ ] SQL 注入防护
- [ ] XSS 防护
- [ ] CSRF 防护

### 凭证管理
- [ ] 无硬编码密钥
- [ ] 敏感配置使用环境变量
- [ ] 日志无敏感信息泄露

### 审查结果
🔴 阻断项：0
🟡 警告项：2
✅ 通过项：12/14
```
